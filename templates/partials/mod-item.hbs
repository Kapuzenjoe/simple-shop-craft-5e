<div class="mod-item" data-tooltip="{{ localize hint }}">
    <i class="{{ icon }}" inert></i>
    <input type="number" name="{{ name }}" value="{{ value }}" min="-100" max="1000" step="1" {{#unless editable}}readonly{{/unless}}>
    <span class="unit">%</span>
</div>
