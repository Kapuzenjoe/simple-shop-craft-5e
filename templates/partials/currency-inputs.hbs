{{#each rows}}
<label>
    <i class="currency {{ this.denomination }}" data-tooltip aria-label="{{ this.label }}"></i>
    {{ numberInput this.value name=this.name min=0 step=1
        disabled=../disabled }}
</label>
{{/each}}
